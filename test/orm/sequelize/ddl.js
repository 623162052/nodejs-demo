const util = require('util');
const CfgCode = require('./cfgcode');
const sequelize = require('./pool');

// create the tables
// CfgCode.sync();

// Force the creation!
// CfgCode.sync({force: true});

// drop the tables
// CfgCode.drop();
